---
scope: SAMS whole system: Core Engine + CLI (sams.py, infovis.py, investigate.py) + Streamlit Web UI
purpose: Consistency contract so 10 teammates build engine/CLI/Web UI units that compose without divergence
altitude: project→epics (PRD + UX spines above, epics/stories below)
updated: 2026-07-10T23:08
---

- (constraint) Inherited (PRD/brief): Python; OpenCV+NumPy (image processing), Matplotlib (charts), SQLite (Local DB), stdlib XML; three exact CLI entry points sams.py/infovis.py/investigate.py with brief-verbatim command forms; fresh-machine install via pip install -r requirements.txt; no OCR, no cloud, no auth
- (constraint) Inherited (PRD FR-15/FR-16 + UX spines): Core Engine owns detection/persistence/visualization/verification; frontends are thin; engine emits labelled stage artifacts via callback/generator, never opens display windows (cv2.imshow forbidden in engine); CLI renders OpenCV windows, Web UI renders in browser; CLI-vs-Web result parity at data layer
- (constraint) Inherited (UX spines, final): Streamlit multipage Web UI (resolves OQ-3); 3 pages Process/Lookup/Investigate; per-sheet stage output folders output/<Sheet Identifier>/NN-stage.png; CLI --overwrite flag semantics; Ambiguous first-class persisted status; one-shot Process semantics fenced from Streamlit reruns
- (constraint) Inherited (PRD 6.3): Web UI gated behind SM-1..SM-3 passing on all 5 sample sheets; fallback deliverable engine+CLI only; glossary terms verbatim in code and docs
- (decision) Working mode: Fast path (user choice). Deliverables: spine + team walkthrough HTML deck + report-ready design section (audience: 10-member group needing alignment + group report design chapter)
- (version) Verified on web 2026-07-10: opencv-python 5.0.0.93 (2026-07-02), streamlit 1.59.1 (2026-07-08), numpy 2.5.1, matplotlib 3.11.x stable; all support Python 3.10-3.13. [ASSUMPTION] pin opencv-python 5.x — if course labs/tutorials target 4.x APIs, pin 4.12.x instead; core API calls used (imread/cvtColor/threshold/findContours) are identical
- (decision) AD paradigm: Ports and Adapters (hexagonal) — sams_core package is the domain (pure Python, no UI framework imports); driving adapters = CLI entry scripts + Streamlit pages; driven adapters = SQLite repository + filesystem artifact writer. Binds: where any new code lives. Prevents: engine logic leaking into frontends, frontends diverging from shared behavior. Partly ADOPTED from PRD FR-15/16
- (decision) AD layout seed: repo root holds brief-verbatim entry points sams.py/infovis.py/investigate.py (thin, <50 lines, parse args + call engine + render); sams_core/ package: config.py, errors.py, models.py, info_file.py, pipeline.py (stages), locate.py (table/cell), detect.py (signature presence), mapping.py, repository.py, visualization.py, verification.py, artifacts.py; webui/ folder: app.py + pages/. Seed only — code owns it after cold start
- (decision) AD data contracts: all cross-module data passes as frozen dataclasses + enums defined ONLY in sams_core/models.py — AttendanceStatus enum (PRESENT/ABSENT/AMBIGUOUS), StudentRecord, AttendanceRecord, StageArtifact(order:int, slug:str, label:str, image:ndarray), SheetResult, VerificationResult(score:float 0-1, matched:bool, threshold:float). Prevents: bare dict/tuple contracts diverging between 10 authors. Rule: no module defines its own cross-boundary shape
- (decision) AD pipeline shape: processing is a generator (or callback-fed runner) yielding StageArtifact in the ONE canonical ordered stage registry in pipeline.py — stage names/slugs/order defined exactly once, glossary-verbatim (original, greyscale, denoised, binarized, deskewed, detected table grid, per-cell inspection). Stages are pure functions ndarray-in/ndarray-out. Prevents: CLI and Web UI disagreeing on stage list/labels/order; enables FR-11+FR-16 from one code path
- (decision) AD state mutation: ALL Local DB access goes through sams_core/repository.py — the only module importing sqlite3. Schema auto-created (ensure_schema on open); attendance upsert keyed (Student Index, Sheet Identifier); operator resolutions carry resolved_by_operator flag and survive re-processing unless overwrite=True passed explicitly (CLI --overwrite flag / Web UI confirm). Prevents: two DB access styles, duplicate rows, silent loss of hand resolutions. DB file path from config.py
- (decision) AD config ownership: every tunable (Ambiguous coverage bands, similarity threshold, binarization/deskew params, DB path, output dir) is a named constant in sams_core/config.py — no numeric literals in stage/verification code. Prevents: sams.py and investigate.py silently using different thresholds; makes report's threshold-tuning discussion one file
- (decision) AD error strategy: exception hierarchy in sams_core/errors.py — SamsError base, InputError (bad image/Info File), ProcessingError (pipeline failure), DataError (unknown Student Index, row-count mismatch carries details). Engine raises, NEVER prints/exits/shows UI. CLI adapters translate to one-line stderr message + exit codes (0 ok, 2 input error, 1 other). Web UI translates to UX error-catalog strings. Prevents: raw stack traces reaching grader, inconsistent error surfaces
- (decision) AD display boundary [ADOPTED FR-16 + UX CLI Display Contract]: sams_core never imports streamlit, never calls cv2.imshow or plt.show; visualization.py returns matplotlib Figure objects; artifacts.py owns saving stage images to output/<Sheet Identifier>/NN-slug.png. CLI renders OpenCV windows + prints per-student stdout summary; Web UI renders st.image/st.pyplot. Prevents: engine unusable headless, double rendering paths
- (decision) AD dependency rule: sams_core + CLI import only cv2/numpy/matplotlib/sqlite3/stdlib (requirements.txt, exact pins); streamlit lives ONLY in requirements-web.txt and webui/ imports. Grader fresh-machine install stays pip install -r requirements.txt with zero web deps [ADOPTED PRD FR-15]. Prevents: web deps breaking the graded install path
- (decision) AD testing seam: pytest; tests import sams_core only (no CLI/web); accuracy harness tests/test_accuracy.py computes SM-1..SM-3 against ground_truth.csv across all five sample sheets — the PRD 6.3 gate is executable. Stage purity gives unit seams. Prevents: gate being judged by eyeball, untestable engine
- (direction) Deferred (story-level, name only): per-stage algorithm choices (Otsu vs adaptive threshold, denoise kernel), similarity metric internals for verification, Streamlit session-state keys beyond the one-shot Process fence, logging framework (stdlib logging default), Info File schema evolution, packaging beyond requirements.txt
- (decision) AD fix (reconcile): row-count mismatch and other non-fatal anomalies are WARNINGS on SheetResult.warnings (processing completes, results shown+flagged) — never exceptions. Exceptions only for cannot-proceed failures. Resolves spine-vs-UX contradiction (error catalog #3: a flag, not a failure)
- (decision) AD fix (reconcile): unknown Student Index is NOT an error — repository exposes list_students(); lookup/verification APIs return a no-data result carrying valid indices; CLI prints friendly list + exit 0; Web renders no-data state. Resolves PRD FR-7 contradiction
- (decision) AD fix (reconcile): signature image data ownership — Reference Signatures live at references/<student_index>/ (filesystem contract, PRD FR-9); probe signature crops extracted during per-cell inspection are saved by artifacts.py under output/<Sheet Identifier>/crops/<student_index>.png; both registered (path metadata) in Local DB via repository.py; retrievable by Student Index. No image blobs in DB
- (decision) AD fix (reconcile): verification API is multi-reference — verification.py compares probe against ALL references for the student and returns VerificationResult(best: ReferenceScore, all_scores: list[ReferenceScore], matched: bool, threshold: float); best-match selection lives in the engine, frontends only render. Disjointness rule: references from sheets 1-3, probes from sheets 4-5 (FR-9/SM-4)
- (decision) AD fix (reconcile): Sheet Identifier resolution chain fixed in engine (info_file.py helper), resolved BEFORE pipeline runs: Info File session date, else operator-supplied (CLI --date flag / Web date field), else image filename stem normalized to ISO 8601 (accepts DD.MM.YYYY). Repository exposes has_operator_resolutions(sheet_id) read API for the pre-Process overwrite warning
- (decision) AD fix (reconcile): SM-C1 anti-overfitting binds AD-5 — no hard-coded pixel coordinates, no per-sheet special-casing, no per-image thresholds; all tunables global in config.py. All filesystem paths relative to project root (fresh-machine portability, FR-15). ground_truth.csv committed to tests/data/ BEFORE threshold tuning (PRD 6.3); Disputed rows excluded from SM-1 scoring. AttendanceRecord carries session/subject metadata. CLI success line reports record count + resolved Sheet Identifier; CLI stage windows shown live during processing, titled with stage name (saving alone insufficient). Deliverable: outer LMS ZIP = prototype zip + MS Word report. OOP convention: cohesive classes for pipeline runner, repository, verifier (graded criterion)
- (change) Reconcile fixes applied: AD-2/4/5/6/7/9 amended in place, AD-10 (signature image ownership) and AD-11 (Sheet Identifier resolution) added; DataError dropped from hierarchy (its cases became warnings/no-data results)
- (decision) Gate fixes (reviewer): StageArtifact.image = display-ready uint8, 2D grayscale or 3-channel RGB; adapters own toolkit conversion (CLI converts RGB-to-BGR for imshow, artifacts.py converts inside imwrite, st.image takes RGB as-is). process_sheet() is the ONE engine entry point both frontends call — it persists on success internally and returns SheetResult; stage emission is a side channel; partial iteration never persists. Similarity score: 0-1, higher = more similar, matched = score >= threshold; distances normalized inside verification.py. Canonical 8-digit Student Index is the ONLY DB key; short ordinal resolved by one engine resolver before any read/write. SQLite: connection per operation via context manager, no cached/global connections (Streamlit threads + CLI lock safety). repository.resolve() write API for operator resolutions incl. undo restore. references/ pre-populated by team from sheets 1-3 (committed); empty references = no-data response. Stage registry emits exactly 7 StageArtifacts (per-cell inspection = one composite overlay); crops are separate artifacts.py outputs. Filename-stem Sheet Identifier fallback is CLI-only (Web requires Info File date or date field). infovis/investigate CLI adapters show Figures via matplotlib show; Web via st.pyplot. Known-index-but-no-probe = no-data variant
- (version) Gate fixes (tech currency): numpy 2.5.1 requires Python >=3.12 — drop 3.10+ claim, target Python 3.12/3.13. Primary opencv pin flipped conservative: opencv-python 4.13.0.92 (current 4.x line; 4.12 was stale), with 5.0.0.93 noted as verified-compatible upgrade (8 days old). matplotlib exact pin 3.11.0. streamlit 1.59.1 confirmed; known wart streamlit#11797 (deep-link + theme) noted as non-blocking
- (event) spine finalized
